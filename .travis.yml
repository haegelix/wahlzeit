language: java
jdk:
  - openjdk8

#before_cache:
#  - rm -f $HOME/.gradle/cache/modules-2/modules-2.lock
#  - rm -fr $HOME/.gradle/cache/*/plugin-resolutions/
#
cache:
  directories:
    - $HOME/.gradle/

install: ./gradlew assemble

script: 
./gradlew test
